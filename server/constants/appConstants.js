const appConfig = require("../config/appConfig");

module.exports = {
  APP_NAME: appConfig.appName,
  COMPANY_NAME: appConfig.company,
  SUPPORT_EMAIL: appConfig.supportEmail,
  WEBSITE: appConfig.website,
  VERSION: appConfig.version,
};